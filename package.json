{
  "name": "app",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build-app": "tsc -b && vite build",
    "build": " tsc -b && vite build && bash build-md.sh",
    "preview": "vite preview"
  },
  "dependencies": {
    "@preact/signals": "^2.3.1",
    "marked": "^16.3.0",
    "preact": "^10.27.2",
    "preact-render-to-string": "^6.6.1",
    "vite-prerender-plugin": "^0.5.12"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.10.2",
    "@types/node": "^24.5.2",
    "typescript": "~5.8.3",
    "vite": "^7.1.2"
  }
}
